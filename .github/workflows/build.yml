name: Build and Release

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            ext: '.exe'
          - os: ubuntu-latest
            ext: ''
          - os: macos-latest
            ext: ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build for Windows
        if: matrix.os == 'windows-latest'
        run: |
          pyinstaller --onefile --name "TankBattle-Windows" main.py

      - name: Build for Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          pyinstaller --onefile --name "TankBattle-Linux" main.py

      - name: Build for macOS
        if: matrix.os == 'macos-latest'
        run: |
          pyinstaller --onefile --name "TankBattle-macOS" main.py

      - name: Create archive for Windows
        if: matrix.os == 'windows-latest'
        run: |
          powershell -Command "Compress-Archive -Path 'dist/TankBattle-Windows.exe' -DestinationPath 'TankBattle-Windows.zip'"
        shell: bash

      - name: Create archive for Linux
        if: matrix.os == 'ubuntu-latest'
        run: tar -czvf "TankBattle-Linux.tar.gz" -C dist TankBattle-Linux
        shell: bash

      - name: Create archive for macOS
        if: matrix.os == 'macos-latest'
        run: tar -czvf "TankBattle-macOS.tar.gz" -C dist TankBattle-macOS
        shell: bash

      - name: Set artifact name for Windows
        if: matrix.os == 'windows-latest'
        run: echo "artifact=TankBattle-Windows.zip" >> $GITHUB_ENV
        shell: bash

      - name: Set artifact name for Linux
        if: matrix.os == 'ubuntu-latest'
        run: echo "artifact=TankBattle-Linux.tar.gz" >> $GITHUB_ENV
        shell: bash

      - name: Set artifact name for macOS
        if: matrix.os == 'macos-latest'
        run: echo "artifact=TankBattle-macOS.tar.gz" >> $GITHUB_ENV
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-build
          path: ${{ env.artifact }}

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Generate checksums
        run: |
          cd artifacts
          find . -type f -exec sha256sum {} \; > checksums.txt
          cat checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}